需求更改记录：

2020.09.03
1. 写文件时数据头部分，输出的通道属性值是什么？
答：需要使用者确保参与计算的3个通道属性一致，然后使用这3个通道的任一通道属性即可。

2. 算法计算结果需要确认。
答：与其他软件计算比对确认。


2020.08.31
1. 输出文件名称增加限制，要求为：
    (1)-文件名称长度30，以后可调
    (2)-文件名应为英文字母开头，无. /~+#^*=\¥等乱七八糟符号
    (3)-文件名和通道名称必须为英文字母，数字以及_-的组合，其他均为非法，必须以字母开头
    (4)-variable name长度应该有限制，请程序员根据udbf格式确认
答：输出编辑框已禁止编辑，使用文件窗口选择的路径和文件窗口中输入的文件名称。所以文件名使用windows规定合法的文件名。

2. 输出通道通道名称限制，长度最大为30个字符长度。
答：目前还没做。

3. 表格和生成按钮之间增大间隙。
答：已完成更改。

4. 单机生成按钮后增加确认按钮。
答：已完成更改。

5. 表格中需要能够多选，能够同时删除多个通道。
答：已完成更改。
